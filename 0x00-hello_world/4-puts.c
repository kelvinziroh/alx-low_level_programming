#include <stdio.h>

/**
 * main - Print a string to standard output
 *
 * Return: 0 if program executes successfully
 */
int main(void)
{
	char quote[] = "\"Programming is like building a multilingual puzzle";

	puts(quote);
	return (0);
}
